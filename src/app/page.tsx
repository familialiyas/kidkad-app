export default function Home() {
  return (
    <div className="flex flex-1 items-center justify-center p-8 text-center text-sm text-gray-400">
      KidKad — landing page not built yet. Try /invite/[guest_link] or /dashboard/[admin_link].
    </div>
  );
}
